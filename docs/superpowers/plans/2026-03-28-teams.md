# Teams (Отделения) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Allow users to create "отделения" (climbing teams) with roles (leader/member), shared gear distribution, shared food rations, invite links, and per-member readiness checklists.

**Architecture:** New database tables for teams, members, shared gear, and readiness. New `/teams` route with team list, create form, and detail page. Team detail page has tabs: Members, Gear, Rations, Readiness. Invite system uses unique tokens stored in the team record. Leader can manage members and see everyone's readiness.

**Tech Stack:** Next.js 15 App Router, React 19, Supabase (RLS), Tailwind CSS 4

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `supabase/migrations/009_teams.sql` | Create | Tables: teams, team_members, team_gear, team_readiness |
| `src/app/teams/page.tsx` | Create | Team list page (server component) |
| `src/app/teams/[id]/page.tsx` | Create | Team detail page (server component) |
| `src/app/teams/join/[token]/page.tsx` | Create | Join team via invite link |
| `src/components/teams/team-list.tsx` | Create | List of user's teams + create button |
| `src/components/teams/team-detail.tsx` | Create | Team detail with tabs: Members, Gear, Rations, Readiness |
| `src/components/teams/create-team-modal.tsx` | Create | Modal for creating a new team |
| `src/components/teams/team-members.tsx` | Create | Members tab: list, roles, invite link, remove |
| `src/components/teams/team-gear.tsx` | Create | Shared gear: who brings what |
| `src/components/teams/team-rations.tsx` | Create | Shared rations for the team |
| `src/components/teams/team-readiness.tsx` | Create | Per-member readiness checklist |
| `src/components/nav/navbar.tsx` | Modify | Add "Отделения" nav link |

---

### Task 1: Database Migration

**Files:**
- Create: `supabase/migrations/009_teams.sql`

- [ ] **Step 1: Create migration file**

```sql
-- Teams
CREATE TABLE teams (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL,
  description text,
  route_id uuid REFERENCES routes ON DELETE SET NULL,
  mountain_id uuid REFERENCES mountains ON DELETE SET NULL,
  start_date date,
  end_date date,
  leader_id uuid REFERENCES profiles ON DELETE CASCADE NOT NULL,
  invite_token text UNIQUE DEFAULT encode(gen_random_bytes(16), 'hex'),
  created_at timestamptz DEFAULT now() NOT NULL,
  updated_at timestamptz DEFAULT now() NOT NULL
);

CREATE TRIGGER teams_updated_at
  BEFORE UPDATE ON teams
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

-- Team members
CREATE TABLE team_members (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  team_id uuid REFERENCES teams ON DELETE CASCADE NOT NULL,
  user_id uuid REFERENCES profiles ON DELETE CASCADE NOT NULL,
  role text CHECK (role IN ('leader', 'member')) DEFAULT 'member',
  joined_at timestamptz DEFAULT now() NOT NULL,
  UNIQUE(team_id, user_id)
);

-- Team gear assignments (who brings what)
CREATE TABLE team_gear (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  team_id uuid REFERENCES teams ON DELETE CASCADE NOT NULL,
  gear_name text NOT NULL,
  category text CHECK (category IN ('ropes', 'hardware', 'clothing', 'footwear', 'bivouac', 'electronics', 'group', 'other')) DEFAULT 'other',
  weight integer,
  quantity integer DEFAULT 1,
  assigned_to uuid REFERENCES profiles ON DELETE SET NULL,
  is_group_gear boolean DEFAULT false,
  created_at timestamptz DEFAULT now() NOT NULL
);

-- Team readiness checklist
CREATE TABLE team_readiness (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  team_id uuid REFERENCES teams ON DELETE CASCADE NOT NULL,
  user_id uuid REFERENCES profiles ON DELETE CASCADE NOT NULL,
  item text NOT NULL,
  checked boolean DEFAULT false,
  created_at timestamptz DEFAULT now() NOT NULL,
  UNIQUE(team_id, user_id, item)
);

-- RLS
ALTER TABLE teams ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Team members can view their teams" ON teams FOR SELECT TO authenticated
  USING (id IN (SELECT team_id FROM team_members WHERE user_id = auth.uid()));
CREATE POLICY "Authenticated users can create teams" ON teams FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = leader_id);
CREATE POLICY "Leaders can update their teams" ON teams FOR UPDATE TO authenticated
  USING (auth.uid() = leader_id);
CREATE POLICY "Leaders can delete their teams" ON teams FOR DELETE TO authenticated
  USING (auth.uid() = leader_id);
-- Allow reading team by invite token (for join page)
CREATE POLICY "Anyone can read team by invite token" ON teams FOR SELECT TO authenticated
  USING (true);

ALTER TABLE team_members ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Team members can view members" ON team_members FOR SELECT TO authenticated
  USING (team_id IN (SELECT team_id FROM team_members tm WHERE tm.user_id = auth.uid()));
CREATE POLICY "Leaders can manage members" ON team_members FOR ALL TO authenticated
  USING (team_id IN (SELECT id FROM teams WHERE leader_id = auth.uid()));
CREATE POLICY "Users can join teams" ON team_members FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can leave teams" ON team_members FOR DELETE TO authenticated
  USING (auth.uid() = user_id);

ALTER TABLE team_gear ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Team members can view gear" ON team_gear FOR SELECT TO authenticated
  USING (team_id IN (SELECT team_id FROM team_members WHERE user_id = auth.uid()));
CREATE POLICY "Team members can manage gear" ON team_gear FOR ALL TO authenticated
  USING (team_id IN (SELECT team_id FROM team_members WHERE user_id = auth.uid()));

ALTER TABLE team_readiness ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Team members can view readiness" ON team_readiness FOR SELECT TO authenticated
  USING (team_id IN (SELECT team_id FROM team_members WHERE user_id = auth.uid()));
CREATE POLICY "Users manage own readiness" ON team_readiness FOR ALL TO authenticated
  USING (auth.uid() = user_id);
CREATE POLICY "Leaders can manage all readiness" ON team_readiness FOR ALL TO authenticated
  USING (team_id IN (SELECT id FROM teams WHERE leader_id = auth.uid()));

-- Indexes
CREATE INDEX idx_team_members_team ON team_members(team_id);
CREATE INDEX idx_team_members_user ON team_members(user_id);
CREATE INDEX idx_team_gear_team ON team_gear(team_id);
CREATE INDEX idx_team_readiness_team ON team_readiness(team_id);
CREATE INDEX idx_teams_leader ON teams(leader_id);
CREATE INDEX idx_teams_invite ON teams(invite_token);
```

- [ ] **Step 2: Apply via Supabase SQL Editor**

- [ ] **Step 3: Commit**

---

### Task 2: Navigation + Pages (shell)

**Files:**
- Modify: `src/components/nav/navbar.tsx`
- Create: `src/app/teams/page.tsx`
- Create: `src/app/teams/[id]/page.tsx`

- [ ] **Step 1: Add nav link** — add `{ href: '/teams', label: 'Отделения', icon: UsersRound }` to navLinks, import UsersRound from lucide-react

- [ ] **Step 2: Create teams list page** — server component, fetch teams where user is a member

- [ ] **Step 3: Create team detail page** — server component, fetch team by id with members

- [ ] **Step 4: Commit**

---

### Task 3: Team List + Create Modal

**Files:**
- Create: `src/components/teams/team-list.tsx`
- Create: `src/components/teams/create-team-modal.tsx`

- [ ] **Step 1: Create team list component** — shows user's teams as cards (team name, route, dates, member count, role badge). Plus "Создать отделение" button.

- [ ] **Step 2: Create modal** — form with: name, description, mountain select (from existing mountains), route select (filtered by mountain), start/end dates. On submit: insert into teams + insert leader as team_member with role 'leader'.

- [ ] **Step 3: Commit**

---

### Task 4: Team Detail + Members Tab

**Files:**
- Create: `src/components/teams/team-detail.tsx`
- Create: `src/components/teams/team-members.tsx`

- [ ] **Step 1: Create team detail** — tabs: Участники, Снаряжение, Раскладка, Готовность. Header with team name, route, dates. Back button.

- [ ] **Step 2: Create members tab** — list of members with role badges (Руководитель / Участник). Invite link (copy button). Leader can remove members. Show member's display_name.

- [ ] **Step 3: Commit**

---

### Task 5: Join Team Page

**Files:**
- Create: `src/app/teams/join/[token]/page.tsx`

- [ ] **Step 1: Create join page** — server component. Lookup team by invite_token. If found: show team name and "Присоединиться" button. On click: insert into team_members, redirect to team detail. If user already a member: redirect to team detail. If not found: show error.

- [ ] **Step 2: Commit**

---

### Task 6: Team Gear Tab

**Files:**
- Create: `src/components/teams/team-gear.tsx`

- [ ] **Step 1: Create team gear component** — shared gear list with columns: Item, Category, Weight, Quantity, Assigned To. Add gear form: name, category, weight, quantity, assign to (dropdown of members). "Групповое" checkbox for group gear (tents, ropes). Total weight display. Any member can add/edit gear.

- [ ] **Step 2: Commit**

---

### Task 7: Team Rations Tab

**Files:**
- Create: `src/components/teams/team-rations.tsx`

- [ ] **Step 1: Create team rations component** — reuse ration templates from `/src/lib/data/ration-templates.json`. Show selector for template, auto-set people count to team member count, days from team dates. Show menu and shopping list. Leader can pick the template. Shopping list items can be assigned to members (who buys what).

- [ ] **Step 2: Commit**

---

### Task 8: Team Readiness Tab

**Files:**
- Create: `src/components/teams/team-readiness.tsx`

- [ ] **Step 1: Create readiness component**

Default checklist items (auto-created for each member):
- "Снаряжение собрано"
- "Еда закуплена"
- "Документы готовы (страховка, пропуск)"
- "Физическая подготовка"
- "Медаптечка"
- "Связь (рация/телефон)"

Display as grid: rows = checklist items, columns = members. Green check / gray circle. Each user can toggle their own items. Leader sees the full grid. Overall team readiness percentage at the top.

- [ ] **Step 2: Commit**
